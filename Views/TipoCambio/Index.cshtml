@model Trabajo_Final.ViewModel.TipoCambioViewModel


@{
    ViewBag.Title = "Tipo Cambio";
    var symbols = ViewData["symbols"] as Dictionary<string, string>;
}

<div class="container">
    <h1 class="text-center mb-4">Realizar conversión de monedas</h1>
    <div class="row justify-content-center">
        <div class="col-md-6">
            <form asp-controller="TipoCambio" asp-action="Exchange" method="post">
                <div class="form-group">
                    <label asp-for="From" class="control-label">DE:</label>
                    <select asp-for="From" class="form-control">
                        @foreach (var symbol in symbols)
                        {
                            <option value="@symbol.Key">@symbol.Value</option>
                        }
                    </select>
                </div>
                <div class="form-group">
                    <label asp-for="To" class="control-label">A:</label>
                    <select asp-for="To" class="form-control">
                        @foreach (var symbol in symbols)
                        {
                            <option value="@symbol.Key">@symbol.Value</option>
                        }
                    </select>

                </div>
                <div class="form-group">
                    <label asp-for="Amount" class="control-label">MONTO:</label>
                    <input asp-for="Amount" class="form-control" />
                </div>
                <br>
                <div class="form-group text-center">
                    <button type="submit" class="btn btn-primary">Realizar Cambio</button>
                </div>
            </form>
            <br>
            <h1>Tipo Cambio: @ViewData["rate"]</h1>
            <h1>Cambio Total: @ViewData["result"]</h1>
            <br>
        </div>
    </div>
</div>